# Prompt log — e263114

## Gap — 2026-09-10 17:01:31 Europe/Rome

Earlier prompts in this conversation were received before the repository instructions became available. Their complete raw message payloads cannot be recovered without reconstruction, so they are not recreated here.

## Entry 1 — 2026-09-10 17:01:31 Europe/Rome

```text
e263114
```

Result: Established the PR #1 foundation, including the Vercel static scaffold, PII-safe data preparation, cleaning documentation, decision log, and repository ignore rules.

## Entry 2 — 2026-09-10 21:04:58 Europe/Rome

```text
PR #1 has been merged into main. Please pull the latest changes from main so you're up to date, then confirm you're ready to start PR #2.
```

+## Entry 3 — 2026-09-10 21:10:00 Europe/Rome

```text
We're starting PR #2 from the approved 6-PR plan: **Decision model and test suite**. PR #1 (foundation, data safety, cleaning contract) is merged into main — please pull main first before doing anything else.

**Scope — implement ONLY this PR. Do not touch the UI, charts, or&#x20;****`src/main.tsx`****.**

Build pure, tested TypeScript functions in `src/lib/model/` that calculate everything the cockpit will need. Read `docs/DECISIONS.md` first — every formula must match the decisions logged there. Specifically:

**1. Acceptance & contribution model**

- Function that takes a price (€1.79 / €2.19 / €2.59) and a channel mix (% D2C, % specialty retail, % modern trade) and returns: acceptance rate, blended contribution per unit, and expected value per prospect.
- Source the raw acceptance/contribution figures from `price_test_results.csv`, using our locked channel mapping: `DTC Online` → D2C, `Gym & Office` → specialty retail, `Retail/Grocery` → modern trade.
- Default scenario: €2.19, 70% D2C / 30% specialty retail / 0% modern trade. This must reproduce the verified numbers from our earlier discussion: \~€0.46 expected value/prospect, \~49 units to recover CAC (using the €44.01 weighted CAC, not a rounded €44).

**2. Acquisition funnel**

- Chain: `budget → reach (CPM from marketing_funnel_monthly.csv) → engaged prospects (engagement rate) → trials (acceptance rate from step 1) → repeat buyers (repeat-purchase rate proxy from historical_sales_weekly.csv, NL/DK/SE, labeled as proxy) → monthly units and revenue`.
- Every rate must be a named constant or function with a comment citing its source exhibit. If a rate isn't in the data, use a clearly-named placeholder (e.g. `PLACEHOLDER_repeatPurchaseRate`) and list it in the function's JSDoc as an assumption needing validation.
- Input: €250,000 budget, 70/30 D2C/specialty split (both overridable parameters, not hardcoded).

**3. Payback & LTV:CAC**

- 24-month projection. Payback = first month where cumulative contribution margin ≥ cumulative marketing spend. Target ≤12 months.
- LTV:CAC calculation, target 3:1.
- Handle the "no payback within 24 months" case explicitly — return a typed result, not NaN or undefined.

**4. Phase 2 gate check**

- Function that takes trailing 3-month metrics and returns which of the 4 gates pass/fail: blended contribution margin ≥35%, blended CAC ≤€50, LTV:CAC ≥2.5:1, D2C repeat-purchase rate ≥25%.

**5. Home-market proxy labeling**

- Every function that uses NL/DK/SE data (not Germany-specific) must return its result wrapped with a `{ value, isProxy: true, proxySource: string }` shape, or equivalent — something PR #3's UI can detect and badge automatically. Don't just note it in a comment; make it inspectable in the return type.

**6. Tests**

- `tests/model/*` using Vitest. Cover: the default €2.19 scenario reproduces our verified numbers, each of the 3 prices calculates correctly, edge cases (0% budget, 100% one channel, no-payback scenario), and that proxy-labeled results are correctly flagged.
- Use the cleaned data from `src/data/generated/` (produced by PR #1's `prepare-data.ts`) as test fixtures — extend that script if you need additional exhibits prepared (e.g. `price_test_results.csv`, `marketing_funnel_monthly.csv`, `channel_economics.csv`, `seasonality_and_weather.csv`). Keep the same PII-safe allowlist pattern for any new source file.

**Non-goals for this PR:** no React components, no charts, no changes to `src/main.tsx` beyond what's strictly necessary to keep the app building. No memo generation. No positioning map. Those are PRs 3–6.

**Before writing code:** briefly confirm your understanding of the 4 required exhibits you'll need to add to `prepare-data.ts`, and flag anything in this scope that's ambiguous or where the source CSVs might not support what I've asked. Then proceed.

When done: run the test suite, make sure `pnpm build` still passes, then push to a new branch (`codex/pr2-decision-model`) and follow the same PR-creation flow as PR #1 — I'll create the PR manually on GitHub since your integration can't do it directly. Include in the PR description: what changed, key formulas and their sources, any placeholder assumptions that need business validation, and test results.
```

## Entry 4 — 2026-09-10 21:15:54 Europe/Rome

```text
Correction accepted — €0.595 expected value / \~38.2 units to recover CAC is the right number under our locked 70/30 D2C/specialty mix. Please update `docs/DECISIONS.md` with a short addendum noting this supersedes the earlier €0.46/49 figures that used an unlocked 50/50 D2C/Retail-Grocery blend, and update the memo-facing headline numbers to €0.595 / \~38 units going forward.
On the dependency blocker: please retry the pnpm install/dependency restore. If it fails again, tell me exactly what command failed and what error it returned, so I can help resolve it outside the sandbox if needed.
Once dependencies are restored, continue building the remaining scope from the original PR #2 prompt: the acquisition funnel, 24-month payback/LTV:CAC calculation, and the Phase 2 gate-check function — plus their tests. Don't commit or open a PR until `pnpm build` and `pnpm test` both pass.
```
